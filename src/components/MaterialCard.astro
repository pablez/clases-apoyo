---
const { material } = Astro.props;
---
---
// Props: material
const { material } = Astro.props;
---
<article class="material-card">
  <header class="card-header">
    <h3>
      {material.materia}
      <span class="badges" style="margin-left:8px;">
        {material.nivel ? <small class="badge nivel">{material.nivel}</small> : null}
        {material.grado ? <small class="badge grado">{material.grado}</small> : null}
      </span>
    </h3>
  </header>
  {material.descripcion ? <p>{material.descripcion}</p> : null}
  {material.url_recurso ? <a class="btn" href={material.url_recurso} target="_blank" rel="noreferrer">Ver recurso</a> : null}
</article>
        {material.materia}
      </span>
    </div>
  </div>
  
  <div class="p-4">
    <h3 class="font-bold text-lg mb-2 line-clamp-2">{material.titulo}</h3>
    <p class="text-gray-600 text-sm mb-4 line-clamp-3">{material.descripcion}</p>
    
    <a
      href={material.url_recurso}
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex items-center text-blue-600 hover:text-blue-700 font-medium text-sm"
    >
      Ver recurso
      <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
      </svg>
    </a>
  </div>
</article>
